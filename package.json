{
	"name": "registry",
	"type": "module",
	"version": "0.8.2",
	"scripts": {
		"dev": "wrangler dev",
		"deploy": "wrangler deploy --minify",
		"cf-typegen": "wrangler types --env-interface CloudflareBindings",
		"format": "biome check --write",
		"registry:build": "shadcn build",
		"build": "pnpm run registry:build && pnpm run format",
		"test": "vitest run",
		"test:watch": "vitest",
		"test:coverage": "vitest run --coverage"
	},
	"packageManager": "pnpm@11.17.0",
	"devDependencies": {
		"@biomejs/biome": "^2.5.5",
		"@edge-runtime/vm": "^5.0.0",
		"@vitest/coverage-v8": "^4.1.10",
		"shadcn": "^4.14.1",
		"typescript": "^7.0.2",
		"vitest": "^4.1.10",
		"wrangler": "^4.114.0"
	},
	"dependencies": {
		"dequal": "^2.0.3"
	}
}
